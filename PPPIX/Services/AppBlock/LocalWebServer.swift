// LocalWebServer.swift
// Removido — a abordagem de web clips foi substituída pelo Screen Time nativo.
import Foundation

final class LocalWebServer {
    static let shared = LocalWebServer()
    private init() {}
    func startIfNeeded() {}
}
